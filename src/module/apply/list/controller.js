/**
 * @file 审批列表
 * @author Lingling Yan yanlingling@baijiahulian.com
 */
define(function (require) {
    var app = require('../app');

    app.controller('applyListCtrl', ['$scope', '$state', 'applyRequest',
        function ($scope, $state, applyRequest) {
        }
    ]);
});